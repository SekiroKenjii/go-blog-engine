version: "2"

sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "schemas"
    gen:
      go:
        package: "sqlc"
        out: "gen"
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false
